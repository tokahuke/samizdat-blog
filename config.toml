baseURL = '/'
languageCode = 'en-us'
title = 'Samizdat: Your content, available'
# Each series and identity has its own browser origin via subdomain
# (`<identity>.<root>` / `series-<key>.<root>`). Absolute paths
# resolve against the entity's own root, so Hugo's default URL shape
# works without any rewriting.
relativeURLs = false

[Params]
  samizdatPublicKey = "fGfgc7ibvwy26U7nHjcaAhYmyLvXl84Ld-qab_0PPJc"
  getSamizdatPublicKey = "r0Km0HptEt6Fhosmy7qxaKxyDtwHkzi0-eYbt1WatdM"
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    noClasses = false
